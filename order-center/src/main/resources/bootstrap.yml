
#这个文件是从远端git仓库拉取用的配置文件,文件名应该为：bootstrap.yml
server:
  port: 20000
eureka:
  instance:
#    hostname: localhost
    ip-address: 10.1.3.30
  client:
      service-url:
          defaultZone: http://192.168.2.149:18888/eureka/
    #      defaultZone: http://${eureka.instance.hostname}:28888/eureka/
spring:
  application:
    name: order-center
  cloud:
    config:
      label: master
      profile: ordercenter_test
#      uri: http://localhost:20012/  #单个配置中心的时候用uri就可以
      discovery:
        enabled: true
        serviceId: config-server
#  profiles:
#    active: test
#eureka:
#  instance:
#    hostname: localhost
#  client:
#    service-url:
#      defaultZone: http://${eureka.instance.hostname}:28888/eureka/